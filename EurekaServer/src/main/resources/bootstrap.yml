spring:
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: docker
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://configserver:8888
