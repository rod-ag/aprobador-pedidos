spring:
  application:
    name: pedidos
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: docker
  application:
    name: pedidos
  cloud:
    config:
      uri: http://configserver:8888
