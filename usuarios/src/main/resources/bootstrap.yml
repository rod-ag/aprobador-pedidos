spring:
  application:
    name: usuarios
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: docker
  application:
    name: usuarios
  cloud:
    config:
      uri: http://configserver:8888
